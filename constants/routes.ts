export const API_ROUTES = {
  LOGIN: '/auth/login',
  REGISTER: '/auth/register',
} as const;

export const APP_ROUTES = {
  HOME: '/',
  LOGIN: '/auth/login',
  REGISTER: '/auth/register',
  DASHBOARD: "/dashboard"
} as const;
